name: Build Flutter APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build_apk:
    name: Build Flutter APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.13.9'
          channel: 'stable'

      - name: Clean & upgrade dependencies
        run: |
          flutter clean
          flutter pub upgrade --major-versions
          flutter pub get

      - name: Generate launcher icons
        run: flutter pub run flutter_launcher_icons:main

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: build/app/outputs/flutter-apk/app-release.apk

      - name: Send email with APK link
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "Flutter APK Build Completed"
          to: shallagehsaeed@gmail.com
          from: ${{ secrets.EMAIL_USERNAME }}
          body: |
            سلام masood جان،
            فایل APK پروژه‌ات با موفقیت ساخته شد 🎉
            می‌تونی از بخش Actions در GitHub و قسمت Artifacts دانلودش کنی.
